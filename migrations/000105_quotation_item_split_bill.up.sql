-- ออกใบเสนอราคาจากบิลลูกค้าได้ "บางส่วนตามน้ำหนัก": ออก 6 จาก 10 บาท ส่วนที่เหลือค้างอยู่
-- ในบิลเดิม ทั้งสองส่วนคิดที่ราคาถัวเดียวกัน ยอดรวมทุกรอบจึงเท่ากับที่ลูกค้าล็อกไว้พอดี
--
-- บิลเดิมเก็บรายการขายของลูกค้าไว้ครบทุกบรรทัด แล้วรับ "บรรทัดหัก" ติดลบหนึ่งบรรทัด
-- (−6 บาท ที่ราคาถัว) ส่วน 6 บาทที่ออกใบไปอยู่ในบิลใหม่ที่แยกออกมา (แบบเดียวกับการแบ่ง
-- บิลตามรายการ) ผลรวมของรายการในบิลเดิมจึงเป็นยอดคงค้างเอง ทุกที่ที่รวมยอดจากรายการ
-- (ภาพรวมรายการขาย, กองรอออกบิลที่แจ้งเตือน LINE, ราคาล็อกหน้าออกใบ) ถูกต้องโดยไม่ต้องแก้
--
-- คอลัมน์นี้บอกว่าบรรทัดไหนเป็นบรรทัดหัก และชี้ไปยังบิลที่รับส่วนนั้นไป — ระบบห้ามลบ/แก้
-- บรรทัดนี้ เพราะลบแล้วลูกค้าจะได้น้ำหนักที่ออกใบไปแล้วคืนกลับมาค้างอีกรอบ
ALTER TABLE quotation_items
    ADD COLUMN IF NOT EXISTS split_bill_id BIGINT;

CREATE INDEX IF NOT EXISTS idx_quotation_items_split_bill
    ON quotation_items (split_bill_id)
    WHERE split_bill_id IS NOT NULL;
